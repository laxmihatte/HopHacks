# Optional. Without it the app routes symptoms with the built-in offline
# matcher in lib/router.ts, which is what the unit tests pin. Set it to put
# Claude in front of that matcher.
ANTHROPIC_API_KEY=
