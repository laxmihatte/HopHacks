import type { NextConfig } from "next";

// Everything is client-side and statically analyzable: no API routes, no server
// actions, no database, no runtime fetching.
const nextConfig: NextConfig = {};

export default nextConfig;
